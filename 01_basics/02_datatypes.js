"use strict"; // treat all JS code as newer version

// alert(3 + 3) // this type of alert is only for browser not node js. here we are using Nodejs.

console.log(3 + 3)
console.log("Prabha")

let name = "Prabha"
let age = 21
let isLoggedIn = false

// Data Types

// Premitive Data types

// Number
// Bigint => big number than Number(int)
// String => '' OR ""
// Boolean => true/false
// null => it is an Stand alone value
// Undefined => 

console.table([typeof(Number), typeof(BigInt), typeof(String), typeof(null), typeof(undefined)])
console.table([typeof(6780876), typeof(456785454654890e65), typeof("prabha"), typeof(null), typeof(undefined)])